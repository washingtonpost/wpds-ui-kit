import { SelectRoot, SelectProps } from "./SelectRoot";
import { SelectTrigger } from "./SelectTrigger";

const Root = SelectRoot;
const Trigger = SelectTrigger;

export const Select = {
  Root,
  Trigger,
};

export type { SelectProps };
