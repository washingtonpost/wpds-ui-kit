import * as React from "react";
import { Tabs as Component } from "./";

export default {
	title: "Tabs",
	component: Component,
};

const Template = (args) => (
  <Component {...args} />
);

export const Tabs = Template.bind({});

Tabs.args = {};
