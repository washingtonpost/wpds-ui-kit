import * as React from "react";
import { Switch as Component } from "./";

export default {
	title: "Switch",
	component: Component,
};

const Template = (args) => (
  <Component {...args} />
);

export const Switch = Template.bind({});

Switch.args = {};
